# @package _global_
# Main Configuration
#
# This is the default configuration loaded by scripts/train.py.
# For experiment-specific configs, see configs/experiments/
#
# Configuration Philosophy:
#   - Defaults are defined in Python factory functions and dataclasses
#   - Experiment configs contain all parameters for reproducibility
#   - YAML files override defaults for specific experiments
#   - To find defaults, check the factory function in the source code

# Load default experiment
defaults:
  - experiments/pqn_cartpole_default

# @package _global_
# Minimal PQN + CCASR config for use as a test base.
agent:
  name: pqn

env:
  name: ccasr-gfp-control

run:
  steps_per_env: 5000
  num_envs: 32
  rollout_steps: 4
  eval_frequency: 100
  eval_rollouts: 16
  eval_max_steps: 288

wandb:
  enabled: false

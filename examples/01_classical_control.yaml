# Evaluation configuration for classical control example
# This demonstrates using a bang-bang controller on the cartpole environment

env:
  name: cartpole-control

agent:
  name: bangbang

run:
  seed: 42
  eval_rollouts: 100
  eval_max_steps: 500
